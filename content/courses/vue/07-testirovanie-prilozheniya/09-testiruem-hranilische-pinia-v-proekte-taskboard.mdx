---
title: "Тестируем хранилище Pinia в проекте TaskBoard"
type: demo
---

# Шаг 1. Создаём файл для теста хранилища колонок

В этой демонстрации мы напишем тесты для хранилища колонок.

Создайте файл `columns.spec.js` в директории `src/stores/__tests__`:

```javascript
import { it, afterEach, describe, beforeEach, expect } from 'vitest'
import '@/stores/__tests__/mockServices'
// Из-за того, что vi.mock всплывает на самый верх файла, у него нет доступа к глобальным переменным
// Поэтому важно делать импорт до использования основными файлами
import { createPinia, setActivePinia } from 'pinia'
import { useColumnsStore } from '@/stores'

describe('columns store', () => {
	let columnsStore
	beforeEach(async () => {
		// Определяем Pinia
		setActivePinia(createPinia())
		columnsStore = useColumnsStore()
		await columnsStore.fetchColumns()
	})
})
```

Обратите внимание: импорт мок-сервисов размещён в самом верху блока импортов. Кроме того, в Vitest каждый элемент тестового API (describe, it, expect и т.д.) необходимо импортировать явно — в отличие от Jest, где они были глобально доступны.

В функции `beforeEach` перед каждым тестом создаётся свежий экземпляр хранилища и загружаются данные из эмулируемого источника.

# Шаг 2. Тестируем получение и создание колонок

Добавим тест для проверки начального количества колонок:

```javascript
import { it, afterEach, describe, beforeEach, expect } from 'vitest'
import '@/stores/__tests__/mockServices'
// Из-за того, что vi.mock всплывает на самый верх файла, у него нет доступа к глобальным переменным
// Поэтому важно делать импорт до использования основными файлами
import { createPinia, setActivePinia } from 'pinia'
import { useColumnsStore } from '@/stores'

describe('columns store', () => {
	let columnsStore
	beforeEach(async () => {
		// Определяем Pinia
		setActivePinia(createPinia())
		columnsStore = useColumnsStore()
		await columnsStore.fetchColumns()
	})
	it('should have initial columns', async () => {
		// Количество загруженных колонок должно соответствовать количеству в нашем файле columns.json
		expect(columnsStore.columns.length).toBe(5)
	})
})
```

Запустить тесты можно командой `npm run test:unit` в директории проекта. Vitest работает в режиме наблюдения (watch mode) и будет автоматически перезапускать тесты при изменении файлов.

Добавим тест для создания новой колонки:

```javascript
import { it, afterEach, describe, beforeEach, expect } from 'vitest'
import '@/stores/__tests__/mockServices'
// Из-за того, что vi.mock всплывает на самый верх файла, у него нет доступа к глобальным переменным
// Поэтому важно делать импорт до использования основными файлами
import { createPinia, setActivePinia } from 'pinia'
import { useColumnsStore } from '@/stores'

describe('columns store', () => {
	let columnsStore
	beforeEach(async () => {
		// Определяем Pinia
		setActivePinia(createPinia())
		columnsStore = useColumnsStore()
		await columnsStore.fetchColumns()
	})
	it('should have initial columns', async () => {
		// Количество загруженных колонок должно соответствовать количеству в файле columns.json
		expect(columnsStore.columns.length).toBe(5)
	})
	it('should add a new column', async () => {
		// В данном тесте неважно, что мы отправляем на сервер. Важно — какой результат нам отдаст mockStore.js
		await columnsStore.addColumn({ title: 'Новая колонка' })
		const columnsLength = columnsStore.columns.length
		// Проверяем, что количество колонок увеличилось на одну
		expect(columnsLength).toBe(6)
		// id новой колонки должно быть 6 (как мы определили в файле mockStore.js)
		expect(columnsStore.columns[columnsLength - 1].id).toBe(6)
	})
})
```

# Шаг 3. Тестируем изменение и удаление колонок

Добавим тест обновления колонки:

```javascript
import { it, afterEach, describe, beforeEach, expect } from 'vitest'
import '@/stores/__tests__/mockServices'
// Из-за того, что vi.mock всплывает на самый верх файла, у него нет доступа к глобальным переменным
// Поэтому важно делать импорт до использования основными файлами
import { createPinia, setActivePinia } from 'pinia'
import { useColumnsStore } from '@/stores'

describe('columns store', () => {
// ...
	it('should update column', async () => {
		const newTitle = 'Наша новая колонка'
		await columnsStore.updateColumn({ id: 1, title: newTitle })
		// Проверяем, что у нас 5 колонок
		expect(columnsStore.columns.length).toBe(5)
		// Проверяем, что изменилось название первой колонки
		expect(columnsStore.columns[0].title).toBe(newTitle)
	})
})
```

И тест удаления колонки:

```javascript
import { it, afterEach, describe, beforeEach, expect } from 'vitest'
import '@/stores/__tests__/mockServices'
// Из-за того, что vi.mock всплывает на самый верх файла, у него нет доступа к глобальным переменным
// Поэтому важно делать импорт до использования основными файлами
import { createPinia, setActivePinia } from 'pinia'
import { useColumnsStore } from '@/stores'

describe('columns store', () => {
// ...
	it('should update column', async () => {
		const newTitle = 'Наша новая колонка'
		await columnsStore.updateColumn({ id: 1, title: newTitle })
		// Проверяем, что у нас 5 колонок
		expect(columnsStore.columns.length).toBe(5)
		// Проверяем, что изменилось название первой колонки
		expect(columnsStore.columns[0].title).toBe(newTitle)
	})
	it('should delete column', async () => {
		await columnsStore.deleteColumn(5)
		// Проверяем, что количество уменьшилось на одну колонку
		expect(columnsStore.columns.length).toBe(4)
		// Проверяем, что колонки с id = 5 не существует
		expect(columnsStore.columns.find(column => column.id == 5)).toBeUndefined()
	})
})
```
