---
title: "Тест по разделу"
type: article
quiz:
  - question: "Какой механизм JavaScript использует Vue.js для перехвата обращений к свойствам объектов и реализации реактивности?"
    type: single
    options:
      - "Object.defineProperty"
      - "Proxy"
      - "MutationObserver"
      - "Event listeners"
    answer: 1
  - question: "Какую роль выполняет функция trigger в системе реактивности Vue.js?"
    type: single
    options:
      - "Регистрирует новый эффект в массиве"
      - "Создаёт обёртку Proxy для объекта"
      - "Находит эффекты, зависящие от изменившегося свойства, и перезапускает их"
      - "Компилирует шаблон в render-функцию"
    answer: 2
  - question: "Какие утверждения о реактивности Vue.js верны?"
    type: multiple
    options:
      - "Vue.js может отслеживать переприсваивание обычных переменных напрямую"
      - "Эффекты лежат в основе работы computed-свойств"
      - "Функция track привязывает свойство к активному эффекту"
      - "Proxy использует обработчики get и set для перехвата операций"
      - "Render-функция анализирует, каким образом используются зависимые переменные"
    answers: [1, 2, 3]
  - question: "Почему Vue.js использует виртуальный DOM вместо прямых манипуляций с реальным DOM?"
    type: single
    options:
      - "Реальный DOM не поддерживает динамические обновления"
      - "Прямые операции с реальным DOM наиболее ресурсоёмкие, а работа с JavaScript-объектами значительно дешевле"
      - "Виртуальный DOM необходим для серверного рендеринга"
      - "Браузеры запрещают прямое изменение DOM из JavaScript"
    answer: 1
  - question: "Для чего используется поле meta в конфигурации маршрута vue-router?"
    type: single
    options:
      - "Для указания HTTP-метода запроса"
      - "Для передачи дополнительных данных в маршрут, например флага авторизации"
      - "Для задания SEO-метатегов страницы"
      - "Для указания типа компонента"
    answer: 1
  - question: "Чем отличается router.push от router.replace?"
    type: single
    options:
      - "push работает только с именованными маршрутами, а replace — с путями"
      - "replace не добавляет запись в историю браузера, и пользователь не сможет вернуться назад"
      - "push выполняет полную перезагрузку страницы, а replace — нет"
      - "Между ними нет разницы, это псевдонимы"
    answer: 1
  - question: "Какие утверждения о vue-router верны?"
    type: multiple
    options:
      - "useRoute() предоставляет информацию о текущем маршруте и его параметрах"
      - "RouterLink автоматически преобразуется в тег <a> при рендеринге"
      - "Ленивая загрузка компонентов реализуется через динамический import()"
      - "Вложенные маршруты не требуют отдельного RouterView"
      - "useRouter() используется для получения параметров текущего URL"
    answers: [0, 1, 2]
  - question: "Где хранится информация о том, какой шаблон (layout) использовать для конкретной страницы?"
    type: single
    options:
      - "В файле конфигурации приложения main.js"
      - "Внутри самого компонента страницы"
      - "В свойстве meta объекта маршрута"
      - "В глобальном хранилище Vuex"
    answer: 2
  - question: "Почему в компоненте-обработчике шаблонов используется shallowRef вместо обычного ref?"
    type: single
    options:
      - "shallowRef работает быстрее при частых обновлениях"
      - "ref не поддерживает хранение компонентов"
      - "Внутреннее значение shallowRef не подвергается глубокому реактивному оборачиванию, что подходит для хранения компонентов"
      - "shallowRef необходим для работы с асинхронными операциями"
    answer: 2
  - question: "Какой динамический элемент Vue.js используется для отображения компонента шаблона, определяемого в runtime?"
    type: single
    options:
      - "<template :is=\"layout\">"
      - "<component :is=\"layout\">"
      - "<slot :name=\"layout\">"
      - "<render :component=\"layout\">"
    answer: 1
  - question: "В каком хуке жизненного цикла директивы лучше всего выполнять начальную настройку элемента после его появления в DOM?"
    type: single
    options:
      - "created"
      - "beforeMount"
      - "mounted"
      - "updated"
    answer: 2
  - question: "Какое свойство объекта binding содержит аргумент директивы (например, значение 'foo' для v-my-directive:foo)?"
    type: single
    options:
      - "binding.value"
      - "binding.modifiers"
      - "binding.arg"
      - "binding.instance"
    answer: 2
  - question: "Какие утверждения о пользовательских директивах Vue верны?"
    type: multiple
    options:
      - "Директивы предназначены для низкоуровневых операций с DOM"
      - "Все хуки жизненного цикла директивы являются обязательными"
      - "Значение и аргумент директивы могут быть динамическими"
      - "Директивы регистрируются с префиксом v- в app.directive()"
      - "Хук prevVnode доступен только в updated и beforeUpdate"
    answers: [0, 2, 4]
  - question: "Какой метод используется для добавления глобального свойства, доступного во всех компонентах приложения Vue?"
    type: single
    options:
      - "app.provide()"
      - "app.config.globalProperties"
      - "app.mixin()"
      - "app.inject()"
    answer: 1
  - question: "Чем глобальная регистрация компонента отличается от локальной?"
    type: single
    options:
      - "Глобально зарегистрированный компонент работает быстрее"
      - "Локально зарегистрированный компонент нельзя использовать в шаблонах"
      - "Глобально зарегистрированный компонент доступен в любом месте приложения без дополнительного импорта"
      - "Глобальная регистрация требует указания входных параметров"
    answer: 2
  - question: "Какой метод объекта плагина вызывается Vue при подключении плагина через app.use()?"
    type: single
    options:
      - "init"
      - "setup"
      - "install"
      - "mount"
    answer: 2
  - question: "Какие три параметра принимает функция h() для создания виртуального узла?"
    type: single
    options:
      - "Имя компонента, слоты, стили"
      - "Тип элемента, объект атрибутов/свойств, дочерние элементы"
      - "HTML-строка, контекст, колбэк"
      - "Тег, CSS-классы, текстовое содержимое"
    answer: 1
  - question: "Как правильно использовать рендер-функцию с <script setup>?"
    type: single
    options:
      - "Экспортировать функцию через export default"
      - "Вызвать функцию h() в хуке onMounted"
      - "Объявить функцию-рендер и использовать её как компонент <render/> в шаблоне"
      - "Передать функцию h() в defineComponent"
    answer: 2
---

# Тест по разделу

Ответьте на вопросы, чтобы проверить усвоение материала.
